content = """# Step 8 – 制造性 / BOM / 文档（DFM & BOM & Documentation）

（以下内容为完整 Step 8 Markdown，无占位符。）

## 1. 步骤定义与成功标准

### 1.1 定义
此步骤关注产品“能否被制造出来”。包含三部分核心能力：  
- **DFM（Design for Manufacturing）**：工艺可制造性检查  
- **BOM 生成与管理**：多级物料清单  
- **制造与装配文档**：工艺卡、说明书、工序步骤等  

### 1.2 成功标准
- 能提出与具体工艺（铣削/钣金/注塑/打印）一致的修改建议  
- 能准确从 CAD/文档抽取多层级 BOM  
- 文档生成结构化、清晰、可用于工厂生产  
- 能被 DFM 工具、供应商或人工审查验证

---

## 2. 具体案例示例

### 示例：桌面级 3D 打印机的制造准备
1. **DFM**：检查钣金边距、折弯半径、孔边距、塑料件壁厚、拔模角  
2. **BOM**：抽取导轨/丝杆/步进电机/皮带/螺钉等，并按装配树生成层级 BOM  
3. **文档**：生成钣金工艺卡、机加工步骤与装配说明

---

## 3. 工具与系统

### 3.1 DFM 工具
- **DFMPro（闭源）**：CAD 内自动可制造性检查  
- **Elysium DFM（闭源）**：加工与模具场景的规则化检查  
- **ProtoLabs / Fictiv（闭源）**：上传 CAD → 自动 DFM + 报价  
- **Autodesk Fusion（闭源）**：集成 DFM + CAM + 机加工可达性分析  

### 3.2 BOM 工具和标准
- **OpenBOM（闭源）**：云端 BOM + PLM  
- **bomfu（开源）**：开源硬件的公共 BOM 库  
- **AutoBOM（开源）**：自动生成 BOM + 静态网页展示  
- **CycloneDX / HBOM（开源）**：通用 BOM 结构标准  

### 3.3 制造文档与 CAM
- **LLM + 开源 CAM（学术开源示例）**：生成加工说明、刀具策略、G-code 草案  
- **SprutCAM X（闭源）**：集成 ChatGPT 的 CAM 助手  

---

## 4. 评测环境

### 4.1 DFM 评测
- 使用“故意违反工艺规则”的 CAD：  
  - 壁厚不足、孔径过小、折弯半径错误、刀具不可达  
- 与 DFMPro/ProtoLabs 报告比对，计算召回率和准确率

### 4.2 BOM 评测
- 使用开源硬件（如 Prusa i3）的公开 BOM  
- 让 Agent 从：  
  - 装配结构  
  - 或 PDF 文档  
  抽取 BOM，再与官方 BOM 比对（行级 F1、层级准确率）

### 4.3 文档评测
- 使用开源项目的装配手册作为 gold  
- 让 Agent 生成制造/装配说明  
- 人工评分正确性/步骤覆盖度/顺序合理性

---

## 5. 可用于训练的数据集

### 5.1 开源硬件（最推荐）
- 例如 Prusa i3  
  - 提供 CAD、BOM、装配说明齐全  
- 可构造训练样本：  
  - 输入：CAD + 简要说明  
  - 输出：BOM + 制造/装配步骤

### 5.2 BOM 开源项目
- bomfu / AutoBOM / CycloneDX 示例数据  
- 可用于训练“BOM 结构化表示”

### 5.3 自建 DFM 数据
- 从公开 DFM 指南抽取规则  
- 分别构造合规/违规几何作为训练集

### 5.4 制造说明数据
- LLM + 开源 CAM 的示例（公开纸箱、家具、无人机等制作流程）  
- 可用于“设计 → 制造说明”训练

---

## 6. 自建评测集（建议）
由你自定义三类任务：

1. **DFM**：100 个有制造缺陷的零件  
2. **BOM**：10 个产品级开源硬件项目  
3. **文档**：10 个装配任务的参考说明  

并提供统一的对比指标（召回率、步骤覆盖度、层级正确率）。

---

## 7. 小结
Step 8 解决“能否在现实中制造”的问题：

- DFM：可制造性与工艺限制  
- BOM：供应链和零件结构化管理  
- 文档：把设计转变为可执行的工厂流程  

是机械设计 Agent 链条进入真实生产前的最后关键环节。

"""

with open('/mnt/data/Step8.md','w') as f:
    f.write(content)

'/mnt/data/Step8.md'
